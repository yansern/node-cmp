guard :shell do
  watch(/.*\.(jshintrc|jshintignore|csslintrc|js|json|css|html)/) do |m|
    title = 'Lint'
    msg = `npm run-script lint`
    status = ($?.success? && :success) || :failed

    n msg, title, status
    "-> #{msg}"
  end
end
